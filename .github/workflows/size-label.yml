name: Size Labeler

on: pull_request

jobs:
  size-label:
    runs-on: ubuntu-latest

    steps:
      - uses: pascalgn/size-label-action@v0.4.3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
